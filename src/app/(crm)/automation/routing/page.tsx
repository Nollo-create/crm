import { AutomationManager } from "@/components/crm/automation-manager";

export default function RoutingPage() {
  return <AutomationManager title="Lead routing" subtitle="Get new, high-intent leads onto someone's task list." category="routing" />;
}
