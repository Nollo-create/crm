import { AutomationManager } from "@/components/crm/automation-manager";

export default function WorkflowsPage() {
  return <AutomationManager title="Workflows" subtitle="Automated rules that turn CRM signals into tasks." />;
}
