import { AutomationManager } from "@/components/crm/automation-manager";

export default function FollowupsPage() {
  return <AutomationManager title="Follow-ups" subtitle="Keep accounts, deals and quotes warm automatically." category="followup" />;
}
